global:
  scrape_interval: 10s
  evaluation_interval: 10s

scrape_configs:
  - job_name: "kafka-connect"
    static_configs:
      - targets: ["kafka-connect:8081"]

  - job_name: "prometheus"
    static_configs:
      - targets: ["localhost:9090"]
